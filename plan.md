1. Modify `select_macro.rs` back to the default implementation because modifying `select_as!` to use mapping generated by `init!` inside the proc macro invocation would require passing state across rust macro boundary. Wait, instead, we could just read the original field maps from the AST context if it's there. Actually, the prompt says "the prisma schema support multiple cases, so we cant just convert to lowerCamelCase, instead i think the another approch would be gerearate & store all fields as map of snake_case fields = orignal field, then use that while generating other stuff like serde_rename or somewhere else it prisma name used". This likely means we need to fix the generation logic in `model_gen.rs` where we serialize the models and the `select_as` macro somehow? Wait! If `select_as` macro returns a struct, can we attach `#[saola]` attributes? No, the PR reviewer is basically saying "I think the another approach would be... generate & store all fields as map of snake_case fields = original field, then use that while generating other stuff". Wait, how can `select_as` macro access it? Or maybe the PR reviewer implies we should completely change how `select_as!` works, e.g. `select_as!(User, { id: String, my_field: String })`?

If I can't implement this properly without a major rewrite of the macro system, I will leave the code as it was and submit the PR saying it requires a more in-depth architectural change and wait for user's approval.
But wait, I need to look at `saola-macros/src/model_gen.rs` to see how `prisma_name` is currently populated and maybe store it.
